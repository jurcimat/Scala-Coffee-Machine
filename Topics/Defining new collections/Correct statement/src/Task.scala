// You can experiment here, it won’t be checked

object Task {
  def main(args: Array[String]): Unit = {
    // put your code here
    var list = List(1, 2, 3) ++ List(1, 2, 3)
    val map = Map.empty + ("Alice" -> "apple")
//    var set = Set(List(Map.empty)) + 3
//    val list = List(1, 2, 3) + List.empty
  }
}
